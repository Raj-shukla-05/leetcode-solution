            revNum = (revNum * 10) + ld;
            x = x / 10;
        }
            int ld = x % 10;
        while (x > 0) {
        long long revNum = 0;
        int dup = x;
    bool isPalindrome(int x) {
public:
        return dup == revNum;
    }
