class Solution {
public:
    int countCommas(int n) {
        int total = 0;
        for (int i = 1; i <= n; i++) {
            if (i >= 1000) total++;
            if (i >= 1000000) total++;
            if (i >= 1000000000) total++;
        }
        return total;
    }
};
