        int span = 1;
    int next(int price) {
    
    }
        
    StockSpanner() {
public:
        while(!st.empty() && st.top().first <= price){
            span += st.top().second;
        }
            st.pop();
        st.push({price, span});
        return span;
    }
