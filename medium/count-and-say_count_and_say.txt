                    count++;
                } else {
                    temp += to_string(count) + result[j - 1];
                    count = 1;
                }
            }
            temp += to_string(count) + result.back();
            result = temp;
        }
        
        return result;
    }
};
