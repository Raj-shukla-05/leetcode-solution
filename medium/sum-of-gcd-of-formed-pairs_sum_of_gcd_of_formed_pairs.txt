    long long gcdSum(vector<int>& nums) {
        int n = nums.size();
        vector<int> prefix(n);

        int mx = nums[0];
        prefix[0] = nums[0];

        for(int i = 1; i < n; i++){
            mx = max(mx, nums[i]);
            prefix[i] = gcd(nums[i], mx);
        }
        sort(prefix.begin(), prefix.end());
        long long sum = 0;
        for(int i = 0; i < n / 2; i++){
