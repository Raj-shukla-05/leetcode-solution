            int u = q.front();
            q.pop();

            for(auto& [v, dist] : adj[u]){
                minEdge = min(minEdge, dist);
                if(!visited[v]){
                    visited[v] = true;
                    q.push(v);
                }
            }
        }
        return minEdge;
    }
};
